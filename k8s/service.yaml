apiVersion: v1
kind: Service
metadata:
  name: daemon-pmac
  labels:
    app: daemon-pmac
spec:
  type: ClusterIP
  selector:
    app: daemon-pmac
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8004
